# Demonstrated: the compile-and-run model (Lesson 1)

Jonas installed Temurin JDK 21, then compiled and ran `Hello.java` successfully
(`javac` → `.class` → `java`). The source → bytecode → JVM pipeline is understood,
not just covered.

**Evidence:** Got `javac`/`java` working from a fresh terminal and reported the
lesson "went well" after running the program.

**Implications:** Toolchain terms (source, bytecode, JVM, JDK, javac, java) are
promoted to [[GLOSSARY.md]]. Ready for Lesson 2 — static typing and variables —
which builds directly on "the compiler checks types before producing bytecode."
