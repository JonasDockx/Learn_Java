# Demonstrated: static typing & variables (Lesson 2)

Jonas completed Lesson 2 — declared typed variables, ran `Types.java`, and
triggered/read the compile error from assigning a `String` to an `int`. Quizzes
passed as expected.

**Evidence:** Reported quizzes and the deliberate compiler error "went as expected."

**Implications:** `int/double/boolean/char/String`, the type-is-fixed rule, and
`var` (inference, not dynamic) are solid. Terms promoted to [[GLOSSARY.md]].
Ready for Lesson 3 — control flow (conditionals + loops) — where the concepts
transfer from Python and the work is mostly syntax + a couple of Java traps
(`{ }`/`( )`, `&&`/`||`, and `==` vs `.equals()` on Strings).
