# Demonstrated: inheritance & polymorphism (Lesson 8)

Jonas completed Lesson 8 — built an `Animal` parent with `Dog`/`Cat` subclasses,
used `super()` and `@Override`, and ran a polymorphic `Animal[]` loop (created
`AnimalDemo.java`). Reported it "went well." Asked forward-looking questions about
`abstract`, composition-vs-inheritance, and override-vs-overload.

**Evidence:** Working polymorphic demo; questions show he's reaching for the design
judgments (when NOT to inherit) rather than just the mechanics.

**Implications:** OO pillars encapsulation/inheritance/polymorphism + override/overload
promoted to [[GLOSSARY.md]]. The three questions were answered and folded into
Lesson 9 (abstraction: abstract classes + interfaces), now in progress. He clearly
grasps is-a; reinforce has-a/composition with a concrete non-animal example when it
recurs. Next after L9: Advanced track begins — exception handling (L10), which he's
already met via `setAge`'s `IllegalArgumentException`.
