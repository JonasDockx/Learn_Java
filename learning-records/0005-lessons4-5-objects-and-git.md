# Demonstrated: references (L4) and local Git (L5)

Jonas proceeded through Lesson 4 (objects/classes/references) and Lesson 5 (Git
basics). On Git he engaged hands-on: edited `NOTES.md` "to see what changed in
Git" and customised `.gitignore` himself (added `.agents/` and `.claude/`,
correctly reasoning they're vendored, not his work).

**Evidence:** Self-directed `.gitignore` edit showing he understood what ignoring
is for; asked sharp follow-ups (`add .` vs naming files, undoing a mistaken
commit, when branches apply) — questions that show he's modelling the workflow,
not just copying commands.

**Implications:** Local Git workflow (init/add/commit/log/.gitignore) is solid;
Git terms promoted to [[GLOSSARY.md]]. Now in Lesson 6 (branching + GitHub).
GitHub part B (account + auth) deferred by design — he has an existing `id_rsa`
SSH key that could be used for GitHub auth when he tackles it. Still owe the OO
thread: encapsulation is the next non-Git lesson.
